name: persistent-arbitrary
version: "0.1.0"
maintainer: Front Row Education

extra-source-files:
  - ../stack.yaml

dependencies:
  - base
  - persistent
  - QuickCheck

library:
  source-dirs:
    - src
  ghc-options: -Wall
