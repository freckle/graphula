name: graphula-core
version: "0.3.0"
maintainer: Front Row Education

extra-source-files:
  - ../stack.yaml

dependencies:
  - HUnit
  - QuickCheck
  - aeson
  - base
  - bytestring
  - containers
  - directory
  - exceptions
  - free
  - generics-eot
  - mtl
  - persistent
  - temporary
  - transformers
  - unliftio
  - unliftio-core

library:
  source-dirs:
    - src
  ghc-options: -Wall

benchmarks:
  bench:
    source-dirs:
      - bench
    main: Main.hs
    ghc-options: -Wall -O2
    dependencies:
      - criterion
      - graphula-core
